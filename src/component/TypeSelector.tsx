export function TypeSelector() {
  return (
    <div>
      <button type='button' value={'ToDo'} />
      <button type='button' value={'Done'} />
      <button type='button' value={'ALL'} />
    </div>
  );
}
